<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#101010">

    <!-- Icono de sin conexión -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_no_wifi"
        android:tint="#555555"
        android:contentDescription="Sin conexión a internet" />

    <!-- Título -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Sin conexión a internet"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textAlignment="center" />

    <!-- Descripción -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="No pudimos conectarnos al servidor.\nVerifica tu conexión a internet e intenta nuevamente."
        android:textColor="#AAAAAA"
        android:textSize="14sp"
        android:textAlignment="center"
        android:lineSpacingExtra="4dp" />

    <!-- Botón de reintentar (Button estándar con estilo personalizado) -->
    <Button
        android:id="@+id/btnReintentar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Reintentar"
        android:textColor="#FFFFFF"
        android:background="@drawable/button_rounded"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textAllCaps="false"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Mensaje adicional -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:textColor="#666666"
        android:textSize="12sp"
        android:textAlignment="center" />

</LinearLayout>